name: demo github actions
on: [push]
jobs:
  say-hello:
   runs-on: ubuntu-latest
   steps:
     - run: echo "hello world"
  say-hello2:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hi world"